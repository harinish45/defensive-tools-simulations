# Defense OS — Defensive Security Simulation Platform
# ------------------------------------------------------------------
# This project runs 100% client-side. NO environment variables are
# required to run it locally — just `npm install && npm run dev`.
#
# This file exists only as documentation for future integrations.
# If you later add live threat-intel sources (VirusTotal, AbuseIPDB,
# etc.), define their keys here and read them from server-side code.
#
# NEXT_PUBLIC_VIRUSTOTAL_API_KEY=
# NEXT_PUBLIC_ABUSEIPDB_API_KEY=
